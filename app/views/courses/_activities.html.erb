<div class="row">
  <div class="col-md-5 pull-right">
    <center>
      <h1><%= t("headers.activity") %></h1>
      <% @activities.each do |activity| %>
        <%= activity.state %>
        <%= activity.created_at %>
      <% end %>
    </center>
  </div>
</div>
